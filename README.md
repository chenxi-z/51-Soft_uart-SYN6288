# 89C5X单片机的模拟串口
* 之前在做设计的时候，碰到过51单片机用挂两个串口设备的情况，查阅资料后，用定时器模拟串口的时序，完成收发工作，达到扩展模拟串口的目的。
---
## 注意
* RXD与TXD用哪两个IO都可以，只要没用就成呗，不止可以扩展一组串口，只要能保证是查询式接收数据，理论上可以随便扩展。
* 但是此方式很占用定时器，因为T1已经给了硬件串口使用，软串口占用了T0，不太友好。
* 没办法，51就这些资源了，时分复用合理分配的话，也不是不能使。
## SYN6288
* 附带SYN6288的驱动，里面用的就是软串口的发送。